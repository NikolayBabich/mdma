# Server configuration
server:
  port: 8080
  servlet:
    context-path: /api
    session.timeout: 30s
  error:
    include-message: always

# Components configuration
system.element:
  dm:
    host: localhost
    port: 8081
